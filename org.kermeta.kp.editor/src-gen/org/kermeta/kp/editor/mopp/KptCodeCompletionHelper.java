/**
 * <copyright>
 * </copyright>
 *
 * 
 */
package org.kermeta.kp.editor.mopp;

// A CodeCompletionHelper can be used to derive completion proposals for partial
// documents. It runs the parser generated by EMFText in a special mode (i.e., the
// rememberExpectedElements mode). Based on the elements that are expected by the
// parser for different regions in the document, valid proposals are computed.
public class KptCodeCompletionHelper {
	
	// Computes a set of proposals for the given document assuming the cursor is
	// at 'cursorOffset'. The proposals are derived using the meta information, i.e.,
	// the generated language plug-in.
	//
	// @param metaInformation
	// @param content the documents content
	// @param cursorOffset
	// @return
	public org.kermeta.kp.editor.mopp.KptCompletionProposal[] computeCompletionProposals(org.kermeta.kp.editor.IKptTextResource originalResource, String content, int cursorOffset) {
		org.eclipse.emf.ecore.resource.ResourceSet resourceSet = new org.eclipse.emf.ecore.resource.impl.ResourceSetImpl();
		// the shadow resource needs the same URI because reference resolvers may use the URI to resolve external references
		org.kermeta.kp.editor.IKptTextResource resource = (org.kermeta.kp.editor.IKptTextResource) resourceSet.createResource(originalResource.getURI());
		java.io.ByteArrayInputStream inputStream = new java.io.ByteArrayInputStream(content.getBytes());
		org.kermeta.kp.editor.mopp.KptMetaInformation metaInformation = resource.getMetaInformation();
		org.kermeta.kp.editor.IKptTextParser parser = metaInformation.createParser(inputStream, null);
		org.kermeta.kp.editor.mopp.KptExpectedTerminal[] expectedElements = parseToExpectedElements(parser, resource);
		if (expectedElements == null) {
			return new org.kermeta.kp.editor.mopp.KptCompletionProposal[0];
		}
		if (expectedElements.length == 0) {
			return new org.kermeta.kp.editor.mopp.KptCompletionProposal[0];
		}
		java.util.List<org.kermeta.kp.editor.mopp.KptExpectedTerminal> expectedAfterCursor = java.util.Arrays.asList(getElementsExpectedAt(expectedElements, cursorOffset));
		java.util.List<org.kermeta.kp.editor.mopp.KptExpectedTerminal> expectedBeforeCursor = java.util.Arrays.asList(getElementsExpectedAt(expectedElements, cursorOffset - 1));
		setPrefixes(expectedAfterCursor, content, cursorOffset);
		setPrefixes(expectedBeforeCursor, content, cursorOffset);
		// first we derive all possible proposals from the set of elements that are expected at the cursor position
		java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> allProposals = new java.util.LinkedHashSet<org.kermeta.kp.editor.mopp.KptCompletionProposal>();
		java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> rightProposals = deriveProposals(expectedAfterCursor, content, resource, cursorOffset);
		java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> leftProposals = deriveProposals(expectedBeforeCursor, content, resource, cursorOffset);
		// second, the set of left proposals (i.e., the ones before the cursor) is
		// checked for emptiness. if the set is empty, the right proposals (i.e., 
		// the ones after the cursor are removed, because it does not make sense to
		// propose them until the element before the cursor was completed
		allProposals.addAll(leftProposals);
		if (leftProposals.isEmpty()) {
			allProposals.addAll(rightProposals);
		}
		// third, the proposals are sorted according to their relevance
		// proposals that matched the prefix are preferred over ones that did not
		// afterward proposals are sorted alphabetically
		final java.util.List<org.kermeta.kp.editor.mopp.KptCompletionProposal> sortedProposals = new java.util.ArrayList<org.kermeta.kp.editor.mopp.KptCompletionProposal>(allProposals);
		java.util.Collections.sort(sortedProposals);
		return sortedProposals.toArray(new org.kermeta.kp.editor.mopp.KptCompletionProposal[sortedProposals.size()]);
	}
	
	public org.kermeta.kp.editor.mopp.KptExpectedTerminal[] parseToExpectedElements(org.kermeta.kp.editor.IKptTextParser parser, org.kermeta.kp.editor.IKptTextResource resource) {
		final java.util.List<org.kermeta.kp.editor.mopp.KptExpectedTerminal> expectedElements = parser.parseToExpectedElements(null, resource);
		if (expectedElements == null) {
			return new org.kermeta.kp.editor.mopp.KptExpectedTerminal[0];
		}
		removeDuplicateEntries(expectedElements);
		removeInvalidEntriesAtEnd(expectedElements);
		return expectedElements.toArray(new org.kermeta.kp.editor.mopp.KptExpectedTerminal[expectedElements.size()]);
	}
	
	private void removeDuplicateEntries(java.util.List<org.kermeta.kp.editor.mopp.KptExpectedTerminal> expectedElements) {
		for (int i = 0; i < expectedElements.size() - 1; i++) {
			org.kermeta.kp.editor.mopp.KptExpectedTerminal elementAtIndex = expectedElements.get(i);
			for (int j = i + 1; j < expectedElements.size();) {
				org.kermeta.kp.editor.mopp.KptExpectedTerminal elementAtNext = expectedElements.get(j);
				if (elementAtIndex.equals(elementAtNext) && elementAtIndex.getStartExcludingHiddenTokens() == elementAtNext.getStartExcludingHiddenTokens()) {
					expectedElements.remove(j);
				} else {
					j++;
				}
			}
		}
	}
	
	private void removeInvalidEntriesAtEnd(java.util.List<org.kermeta.kp.editor.mopp.KptExpectedTerminal> expectedElements) {
		for (int i = 0; i < expectedElements.size() - 1;) {
			org.kermeta.kp.editor.mopp.KptExpectedTerminal elementAtIndex = expectedElements.get(i);
			org.kermeta.kp.editor.mopp.KptExpectedTerminal elementAtNext = expectedElements.get(i + 1);
			if (elementAtIndex.getStartExcludingHiddenTokens() == elementAtNext.getStartExcludingHiddenTokens() && shouldRemove(elementAtIndex.getFollowSetID(), elementAtNext.getFollowSetID())) {
				expectedElements.remove(i + 1);
			} else {
				i++;
			}
		}
	}
	
	public boolean shouldRemove(int followSetID1, int followSetID2) {
		return followSetID1 != followSetID2;
	}
	
	private String findPrefix(java.util.List<org.kermeta.kp.editor.mopp.KptExpectedTerminal> expectedElements, org.kermeta.kp.editor.mopp.KptExpectedTerminal expectedAtCursor, String content, int cursorOffset) {
		if (cursorOffset < 0) {
			return "";
		}
		int end = 0;
		for (org.kermeta.kp.editor.mopp.KptExpectedTerminal expectedElement : expectedElements) {
			if (expectedElement == expectedAtCursor) {
				final int start = expectedElement.getStartExcludingHiddenTokens();
				if (start >= 0  && start < Integer.MAX_VALUE) {
					end = start;
				}
				break;
			}
		}
		end = Math.min(end, cursorOffset);
		final String prefix = content.substring(end, Math.min(content.length(), cursorOffset));
		return prefix;
	}
	
	private java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> deriveProposals(java.util.List<org.kermeta.kp.editor.mopp.KptExpectedTerminal> expectedElements, String content, org.kermeta.kp.editor.IKptTextResource resource, int cursorOffset) {
		java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> resultSet = new java.util.HashSet<org.kermeta.kp.editor.mopp.KptCompletionProposal>();
		for (org.kermeta.kp.editor.mopp.KptExpectedTerminal expectedElement : expectedElements) {
			resultSet.addAll(deriveProposals(expectedElement, content, resource, cursorOffset));
		}
		return resultSet;
	}
	
	private java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> deriveProposals(org.kermeta.kp.editor.mopp.KptExpectedTerminal expectedTerminal, String content, org.kermeta.kp.editor.IKptTextResource resource, int cursorOffset) {
		org.kermeta.kp.editor.mopp.KptMetaInformation metaInformation = resource.getMetaInformation();
		org.kermeta.kp.editor.IKptLocationMap locationMap = resource.getLocationMap();
		org.kermeta.kp.editor.IKptExpectedElement expectedElement = (org.kermeta.kp.editor.IKptExpectedElement) expectedTerminal.getTerminal();
		if (expectedElement instanceof org.kermeta.kp.editor.mopp.KptExpectedCsString) {
			org.kermeta.kp.editor.mopp.KptExpectedCsString csString = (org.kermeta.kp.editor.mopp.KptExpectedCsString) expectedElement;
			return deriveProposal(csString, content, expectedTerminal.getPrefix(), cursorOffset);
		} else if (expectedElement instanceof org.kermeta.kp.editor.mopp.KptExpectedStructuralFeature) {
			org.kermeta.kp.editor.mopp.KptExpectedStructuralFeature expectedFeature = (org.kermeta.kp.editor.mopp.KptExpectedStructuralFeature) expectedElement;
			org.eclipse.emf.ecore.EStructuralFeature feature = expectedFeature.getFeature();
			org.eclipse.emf.ecore.EClassifier featureType = feature.getEType();
			java.util.List<org.eclipse.emf.ecore.EObject> elementsAtCursor = locationMap.getElementsAt(cursorOffset);
			org.eclipse.emf.ecore.EObject container = null;
			// we need to skip the proxy elements at the cursor, because they are not the container for the reference we try to complete
			for (int i = 0; i < elementsAtCursor.size(); i++) {
				container = elementsAtCursor.get(i);
				if (!container.eIsProxy()) {
					break;
				}
			}
			if (feature instanceof org.eclipse.emf.ecore.EReference) {
				org.eclipse.emf.ecore.EReference reference = (org.eclipse.emf.ecore.EReference) feature;
				if (featureType instanceof org.eclipse.emf.ecore.EClass) {
					if (reference.isContainment()) {
						// the FOLLOW set should contain only non-containment references
						assert false;
					} else {
						return handleNCReference(metaInformation, container, reference, expectedTerminal.getPrefix());
					}
				}
			} else if (feature instanceof org.eclipse.emf.ecore.EAttribute) {
				org.eclipse.emf.ecore.EAttribute attribute = (org.eclipse.emf.ecore.EAttribute) feature;
				if (featureType instanceof org.eclipse.emf.ecore.EEnum) {
					org.eclipse.emf.ecore.EEnum enumType = (org.eclipse.emf.ecore.EEnum) featureType;
					return deriveProposals(expectedTerminal, enumType, content, cursorOffset);
				} else {
					// handle EAttributes (derive default value depending on
					// the type of the attribute, figure out token resolver, and
					// call deResolve())
					return handleAttribute(metaInformation, expectedFeature, container, attribute, expectedTerminal.getPrefix());
				}
			} else {
				// there should be no other subclass of EStructuralFeature
				assert false;
			}
		} else {
			// there should be no other class implementing IExpectedElement
			assert false;
		}
		return java.util.Collections.emptyList();
	}
	
	private java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> deriveProposals(org.kermeta.kp.editor.mopp.KptExpectedTerminal expectedElement, org.eclipse.emf.ecore.EEnum enumType, String content, int cursorOffset) {
		java.util.Collection<org.eclipse.emf.ecore.EEnumLiteral> enumLiterals = enumType.getELiterals();
		java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> result = new java.util.HashSet<org.kermeta.kp.editor.mopp.KptCompletionProposal>();
		for (org.eclipse.emf.ecore.EEnumLiteral literal : enumLiterals) {
			String proposal = literal.getLiteral();
			String prefix = expectedElement.getPrefix();
			if (matches(proposal, prefix)) {
				result.add(new org.kermeta.kp.editor.mopp.KptCompletionProposal(proposal, prefix, !"".equals(prefix), true));
			}
		}
		return result;
	}
	
	private java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> handleNCReference(org.kermeta.kp.editor.mopp.KptMetaInformation metaInformation, org.eclipse.emf.ecore.EObject container, org.eclipse.emf.ecore.EReference reference, java.lang.String prefix) {
		// proposals for non-containment references are derived by calling the
		// reference resolver switch in fuzzy mode.
		org.kermeta.kp.editor.IKptReferenceResolverSwitch resolverSwitch = metaInformation.getReferenceResolverSwitch();
		org.kermeta.kp.editor.IKptReferenceResolveResult<org.eclipse.emf.ecore.EObject> result = new org.kermeta.kp.editor.mopp.KptReferenceResolveResult<org.eclipse.emf.ecore.EObject>(true);
		resolverSwitch.resolveFuzzy(prefix, container, reference, 0, result);
		java.util.Collection<org.kermeta.kp.editor.IKptReferenceMapping<org.eclipse.emf.ecore.EObject>> mappings = result.getMappings();
		if (mappings != null) {
			java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> resultSet = new java.util.HashSet<org.kermeta.kp.editor.mopp.KptCompletionProposal>();
			for (org.kermeta.kp.editor.IKptReferenceMapping<org.eclipse.emf.ecore.EObject> mapping : mappings) {
				final String identifier = mapping.getIdentifier();
				// the proposal can be added without checking the prefix because this is
				// performed by the reference resolvers
				resultSet.add(new org.kermeta.kp.editor.mopp.KptCompletionProposal(identifier, prefix, true, true));
			}
			return resultSet;
		}
		return java.util.Collections.emptyList();
	}
	
	private java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> handleAttribute(org.kermeta.kp.editor.mopp.KptMetaInformation metaInformation, org.kermeta.kp.editor.mopp.KptExpectedStructuralFeature expectedFeature, org.eclipse.emf.ecore.EObject container, org.eclipse.emf.ecore.EAttribute attribute, java.lang.String prefix) {
		java.lang.Object defaultValue = getDefaultValue(attribute);
		if (defaultValue != null) {
			org.kermeta.kp.editor.IKptTokenResolverFactory tokenResolverFactory = metaInformation.getTokenResolverFactory();
			String tokenName = expectedFeature.getTokenName();
			if (tokenName != null) {
				org.kermeta.kp.editor.IKptTokenResolver tokenResolver = tokenResolverFactory.createTokenResolver(tokenName);
				if (tokenResolver != null) {
					String defaultValueAsString = tokenResolver.deResolve(defaultValue, attribute, container);
					java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> resultSet = new java.util.HashSet<org.kermeta.kp.editor.mopp.KptCompletionProposal>();
					if (matches(defaultValueAsString, prefix)) {
						resultSet.add(new org.kermeta.kp.editor.mopp.KptCompletionProposal(defaultValueAsString, prefix, !"".equals(prefix), true));
					}
					return resultSet;
				}
			}
		}
		return java.util.Collections.emptyList();
	}
	
	private java.lang.Object getDefaultValue(org.eclipse.emf.ecore.EAttribute attribute) {
		String typeName = attribute.getEType().getName();
		if ("EString".equals(typeName)) {
			return "some" + org.kermeta.kp.editor.util.KptStringUtil.capitalize(attribute.getName());
		}
		return attribute.getDefaultValue();
	}
	
	private java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> deriveProposal(org.kermeta.kp.editor.mopp.KptExpectedCsString csString, String content, String prefix, int cursorOffset) {
		String proposal = csString.getValue();
		java.util.Collection<org.kermeta.kp.editor.mopp.KptCompletionProposal> result = new java.util.HashSet<org.kermeta.kp.editor.mopp.KptCompletionProposal>();
		if (matches(proposal, prefix)) {
			result.add(new org.kermeta.kp.editor.mopp.KptCompletionProposal(proposal, prefix, !"".equals(prefix), false));
		}
		return result;
	}
	
	// for each given expected elements the prefix is calculated
	// the prefix depends on the current document content, the cursor position, and
	// the position where the element is expected
	private void setPrefixes(java.util.List<org.kermeta.kp.editor.mopp.KptExpectedTerminal> expectedElements, String content, int cursorOffset) {
		if (cursorOffset < 0) {
			return;
		}
		for (org.kermeta.kp.editor.mopp.KptExpectedTerminal expectedElement : expectedElements) {
			java.lang.String prefix = findPrefix(expectedElements, expectedElement, content, cursorOffset);
			expectedElement.setPrefix(prefix);
		}
	}
	
	public org.kermeta.kp.editor.mopp.KptExpectedTerminal[] getElementsExpectedAt(org.kermeta.kp.editor.mopp.KptExpectedTerminal[] allExpectedElements, int cursorOffset) {
		java.util.List<org.kermeta.kp.editor.mopp.KptExpectedTerminal> expectedAtCursor = new java.util.ArrayList<org.kermeta.kp.editor.mopp.KptExpectedTerminal>();
		for (int i = 0; i < allExpectedElements.length; i++) {
			org.kermeta.kp.editor.mopp.KptExpectedTerminal expectedElement = allExpectedElements[i];
			int startIncludingHidden = expectedElement.getStartIncludingHiddenTokens();
			int end = getEnd(allExpectedElements, i);
			if (cursorOffset >= startIncludingHidden && cursorOffset <= end) {
				expectedAtCursor.add(expectedElement);
			}
		}
		return expectedAtCursor.toArray(new org.kermeta.kp.editor.mopp.KptExpectedTerminal[expectedAtCursor.size()]);
	}
	
	private int getEnd(org.kermeta.kp.editor.mopp.KptExpectedTerminal[] allExpectedElements, int indexInList) {
		org.kermeta.kp.editor.mopp.KptExpectedTerminal elementAtIndex = allExpectedElements[indexInList];
		int startIncludingHidden = elementAtIndex.getStartIncludingHiddenTokens();
		int startExcludingHidden = elementAtIndex.getStartExcludingHiddenTokens();
		for (int i = indexInList + 1; i < allExpectedElements.length; i++) {
			org.kermeta.kp.editor.mopp.KptExpectedTerminal elementAtI = allExpectedElements[i];
			int startIncludingHiddenForI = elementAtI.getStartIncludingHiddenTokens();
			int startExcludingHiddenForI = elementAtI.getStartExcludingHiddenTokens();
			if (startIncludingHidden != startIncludingHiddenForI || startExcludingHidden != startExcludingHiddenForI) {
				return startIncludingHiddenForI - 1;
			}
		}
		return Integer.MAX_VALUE;
	}
	
	private boolean matches(java.lang.String proposal, java.lang.String prefix) {
		return (proposal.startsWith(prefix) || org.kermeta.kp.editor.util.KptStringUtil.matchCamelCase(prefix, proposal) != null) && !proposal.equals(prefix);
	}
	
}
